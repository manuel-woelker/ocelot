refactor(cli): parse arguments through the PAL

Add an `args` method to the platform abstraction layer and implement it for the
real, mock, and capturing PAL variants. Update the CLI entrypoint to construct
one real PAL handle up front, parse arguments through that PAL, and reuse the
same handle for execution so argument parsing can be covered by tests with
`PalMock`.

User Prompts:
- add an args method to the pal, and instantiate a real_pal in the cli code, the args parsing should use that pal method so it can be tested
- commit

Model:
- GPT-5 Codex
