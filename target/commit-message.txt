feat(spec): validate full rendered diagnostics

Update spec validation to compare full rendered compilation diagnostics
instead of only the headline. Keep synthetic spec sources entirely in the
PAL-backed in-memory overlay and use the stable fictitious filename
`spec-test.ocelot` so rendered diagnostic paths stay deterministic.

User Prompts:
- the expected error spec for the lexer error should be the whole formatted output, including unicode drawing symbols produce by diagnostics rendering
- the fictitious name for all synthetic spec source files should be "spec-test.ocelot"
- remove the /tmp/spec-validation/ prefix as well
- the validator should not write temp files but use the pal abstraction to create in memory files
- nao check
- commit

Model:
- GPT-5 Codex
