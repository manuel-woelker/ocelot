refactor(cli): inline command dispatch

Simplify the CLI entrypoint by inlining command dispatch into `run_command`
and removing the intermediate command types and helper function. The CLI now
parses PAL-provided arguments with `pico-args`, dispatches directly to script
or test execution, and keeps the tests focused on the PAL-backed execution
path.

User Prompts:
- remove the CliCommand env and use the pico-args subcommand method to determine what subcommand to run
- do not use a struct, instead of parsing the command, determine the correct command, and then execute the correct function directly
- put the usage path into run_command as well
- put it in run_command_inner
- run_command_inner should not take closures as arguments, just the pal, then simplify the call
- inline run_command_inner
- commit

Model:
- GPT-5 Codex
