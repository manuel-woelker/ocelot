feat(cli): support running tests across multiple files

Allow `ocelot test` to accept zero or more script paths.
When no paths are provided, discover all `.ocelot` files and run tests across
the full set. When paths are provided, run tests for each requested file and
merge the summaries.
Simplify CLI argument parsing by consuming the raw PAL argument list directly.

User Prompts:
- take a good look at crates/cli/src/main.rs and simplify the parsing and execution
- remove the duplicate pal arguments
- the test subcommand should take 0 - n file arguments, if no file arguments are given, find all ocelot files and run all tests, if 1 or more files are given, run all these tests
- commit

Model:
- GPT-5 Codex
