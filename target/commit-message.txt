feat(spec): specify unterminated string lexer errors

Add a lexer error chapter to the spec with an unterminated string example
and expected diagnostic headline. Tighten the lexer so string literals also
fail when they reach a line break without a closing quote, and add a focused
lexer test for that case.

User Prompts:
- add a chapter 91 to specs to specify lexer errors, add a spec file with an example for an unterminated string, and it's expected error output
- nao check
- commit

Model:
- GPT-5 Codex
