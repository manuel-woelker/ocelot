refactor(cli): simplify command parsing and execution

Unify CLI parsing around a small command enum and a single execution path.
Remove the duplicate PAL arguments from run_command so argument parsing and
execution both use the same handle.
Add focused parser tests for run and test command handling.

User Prompts:
- take a good look at crates/cli/src/main.rs and simplify the parsing and execution
- remove the duplicate pal arguments
- commit

Model:
- GPT-5 Codex
